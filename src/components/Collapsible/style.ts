import styled from 'styled-components'

export const FoodContainer = styled.button`
    &::selection {
        background-color: transparent;
    }

    &:hover {
        cursor: pointer;
    }
`
